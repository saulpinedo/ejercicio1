<%= form_for(@ciudad, :html =>{:class=>  'form-horizontal'}) do |f| %>
  <% if @ciudad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ciudad.errors.count, "error") %> prohibited this ciudad from being saved:</h2>

      <ul>
      <% @ciudad.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :nombre,class:'col-sm-2 control-label' %>
    <div class="col-sm-10">
      <%= f.text_field :nombre,class:'form-control' %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label 'Pais',class:'col-sm-2 control-label' %>
    <div class="col-sm-10">
      <%= f.collection_select :pai_id,Pai.all,:id,:nombre,class:'form-control' %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-2 control-label">
    </div>
    <div class="col-sm-10">
      <%= f.submit 'Crear Ciudad', :class=>'btn btn-primary'%>
    </div>
  </div>
<% end %>
