<%= form_for(@pai,:html => {:class=>'form-horizontal' }) do |f| %>
  <% if @pai.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pai.errors.count, "error") %> prohibited this pai from being saved:</h2>

      <ul>
      <% @pai.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :nombre,class:'col-sm-2 control-label' %>
    <div class="col-sm-10">
         <%= f.text_field :nombre,class: 'form-control' %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label :descripcion, class:'col-sm-2 control-label' %>
    <div class="col-sm-10">
        <%= f.text_area :descripcion,class: 'form-control' %>
    </div>
  </div>

   <div class="form-group">
     <div class="col-sm-2 control-label">
     </div>
   <div class="col-sm-10">
     <%= f.submit 'crear nuevo pais',:class=>'btn btn-primary'%>
   </div>
   </div>

<% end %>